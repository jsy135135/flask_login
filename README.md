### 启动方式
>Linux
```shell
python -m venv ./venv
source ./venv/Scripts/active
pip install -r requirements.txt
python app.py
```

>windows
```shell
python -m venv ./venv
./venv/Scripts/active
pip install -r requirements.txt
python app.py
```